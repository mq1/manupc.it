---
import Layout from "./Layout.astro";

const { frontmatter } = Astro.props;
---

<Layout title={`ManuPC | ${frontmatter.title}`}>
  <article class="prose lg:prose-xl dark:prose-invert sm:p-8 mx-auto">
    <slot />
  </article>
</Layout>
